{
  "name": "zinema-mobile",
  "version": "1.0.0",
  "description": "Zinema.lk Mobile App - Sinhala Movie Streaming",
  "main": "www/index.html",
  "scripts": {
    "build": "echo 'No build step needed - static files in www/'",
    "cap:init": "npx cap init \"Zinema.lk\" \"lk.zinema.app\" --web-dir www",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:build:debug": "npx cap sync && cd android && .\\gradlew assembleDebug",
    "setup": "npm install && npx cap add android && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/network": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.9.3"
  },
  "author": "Zinema.lk",
  "license": "UNLICENSED"
}
