# WhatsApp Bot Configuration
# This bot uses API Bridge to connect to Namecheap database

# API Bridge Configuration (REQUIRED)
# URL to your API Bridge on Namecheap hosting
API_URL=https://zinema.lk/api/whatsapp

# Secret API key - MUST match the key in api/whatsapp/config.php
API_KEY=WA_BOT_2024_ZINEMA_SECURE_KEY_CHANGE_ME

# WhatsApp Storage Group ID (single group)
# Send "!groupid" in your storage group to get this
STORAGE_GROUP_ID=120363404925435399@g.us

# Multiple Storage Groups (comma-separated)
# For better scalability with many movies, use multiple groups
# The bot auto-detects the correct group from the message ID
# Example: STORAGE_GROUP_IDS=120363404925435399@g.us,120363412345678901@g.us
STORAGE_GROUP_IDS=

# Bot Configuration
TOKEN_PREFIX=!get

# Admin phone numbers (comma-separated, without + or spaces)
# Admin phone numbers (comma-separated, without + or spaces)
ADMIN_NUMBERS=94778633953

# Cache Refresher Configuration
# Empty group where files are forwarded to keep them "hot" in WhatsApp cache
# Send "!groupid" in your cache group to get this
CACHE_GROUP_ID=

# How often to refresh popular content (in hours)
# Default: 24 hours (daily at 12:30 AM)
REFRESH_INTERVAL_HOURS=24
